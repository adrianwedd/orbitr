# Supported Python: 3.9–3.11
#
# numpy==1.24.3 / scipy==1.11.4 are pinned to versions that audiocraft==1.3.0
# (which constrains numpy<2.0) is known to work with. These exact versions do
# NOT ship wheels for Python 3.12+, and relaxing them risks breaking the
# audiocraft/torch dependency graph, so the pins are kept intentionally. Use a
# Python 3.9–3.11 interpreter for the full (AI-enabled) backend.
audiocraft==1.3.0
fastapi==0.120.2
uvicorn==0.35.0
python-multipart==0.0.27
pydantic==2.12.3
numpy==1.24.3
scipy==1.11.4
torch>=2.0.0
torchaudio>=2.0.0
transformers>=5.9.0
python-dotenv==1.2.2

# Security dependencies
slowapi==0.1.9
# cryptography provides Fernet, used by api_key_management.py for encrypted key storage.
cryptography>=42.0.0
bleach==6.3.0
